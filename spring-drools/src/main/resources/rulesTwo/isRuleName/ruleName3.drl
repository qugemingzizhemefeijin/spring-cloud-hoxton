package rulesTwo.isRuleName

import com.atguigu.drools.pojo.Person;
import com.atguigu.drools.pojo.School;

rule "abc"
   when
   then
       System.out.println("调用规则"+drools.getRule().getName());
end

rule "123"
   when
   then
       System.out.println("调用规则"+drools.getRule().getName());
end